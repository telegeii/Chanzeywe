# Uploaded applicant documents (KCSE slips, IDs) contain personal data and
# must never be served directly — they only ever leave the server through
# the authenticated download endpoint in applications.php.
Require all denied
